# Copy to front_end/.env (git-ignored) and adjust.
# Requires a full restart of `npm run dev` after changing.
# Point at the deployed backend (Render) instead of the local FastAPI:
VITE_API_URL=https://code-atlas-yupq.onrender.com
